import { ecommUtilFormatters } from './ecomm-util-formatters';

describe('ecommUtilFormatters', () => {
  it('should work', () => {
    expect(ecommUtilFormatters()).toEqual('ecomm-util-formatters');
  });
});
